server:
  port: 8080

spring:
  datasource:
    username: root
    password: 
    url: jdbc:mysql:///extrace
  redis:
    port: 6379
    host: localhost

logging:
  level:
    com.extrace: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1

